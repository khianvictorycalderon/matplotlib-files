# Matplotlib Files
List of python files with matplotlib library. (For testing purposes only)

## To run a specific file:
1. Just run `py <file-name>` or `python <file-name>`. Example: `py hello-world.py`